<% content_for :body do %>
  <ul id="places">
    <% @places.each do |place| %>
      <li>
        <%= link_to place.name, refinery.events_place_path(place) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
