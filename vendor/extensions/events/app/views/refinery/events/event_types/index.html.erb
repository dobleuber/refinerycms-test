<% content_for :body do %>
  <ul id="event_types">
    <% @event_types.each do |event_type| %>
      <li>
        <%= link_to event_type.name, refinery.events_event_type_path(event_type) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
