<%= render 'refinery/admin/search_header', :url => refinery.locationimages_admin_locationimages_path %>
<div class='pagination_container'>
  <% if @locationimages.any? %>
    <%= render 'locationimages' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
