<% content_for :body do %>
  <ul id="menus">
    <% @menus.each do |menu| %>
      <li>
        <%= link_to menu.title, refinery.menus_menu_path(menu) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
